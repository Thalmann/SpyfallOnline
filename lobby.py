from game_info import GameInfo

class Lobby:
    'A lobby of players before actually starting the game managed by a host.'

    def __init__(self, host):
        _players = list()
        self.host = host
        self.players = players

    def join(player):
        players.append(player)

    def kick(player):
        players.remove(player)

    def leave(player):
        players.remove(player)

    def start_game():
        return GameInfo()
